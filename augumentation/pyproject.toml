[project]
name = "concept-clip-augmentation"
version = "0.1.0"
requires-python = "==3.10.*"
dependencies = [
    "torch==2.4.1",
    "torchvision==0.19.1",
    "transformers>=4.40,<5.0",
    "Pillow",
    "numpy",
    "opencv-python",
    "tqdm",
]

[tool.uv]
package = false
